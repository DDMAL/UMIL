apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solr-data-staging
  namespace: umil
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi