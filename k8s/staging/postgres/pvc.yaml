apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-umil-staging-pvc
  namespace: postgres
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi