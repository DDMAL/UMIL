apiVersion: v1
kind: Service
metadata:
  name: postgres-umil-staging
  namespace: postgres
spec:
  selector:
    app: postgres-umil-staging
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP